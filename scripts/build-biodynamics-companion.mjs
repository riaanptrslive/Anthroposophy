import fs from 'node:fs';
import {chapters,sessions,highlights,glossary,evidence,limits} from '../content/biodynamics-companion.mjs';
const out='docs/biodynamics/companion';
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const inventory=fs.readFileSync('content/biodynamics-source-inventory.tsv','utf8').trim().split(/\r?\n/).slice(1).map(line=>{const [p,raw]=line.split('\t');return {page:+p,concepts:raw.split('; ').map((text,i)=>({id:`C${p.padStart(3,'0')}.${i+1}`,text}))};});
const notes=new Map([...fs.readFileSync('content/biodynamics-page-notes.md','utf8').matchAll(/^## Page (\d+)\r?\n([\s\S]*?)(?=^## Page |$(?![\s\S]))/gm)].map(m=>[+m[1],m[2].trim()]));
const lessonFor=p=>sessions.find(s=>p>=s.start&&p<=s.end);
const pageLink=p=>`session-${lessonFor(p).id}.html#page-${p}`;
const para=s=>s.split(/\r?\n\r?\n/).map(p=>`<p>${esc(p)}</p>`).join('\n');
const concepts=inventory.flatMap(p=>p.concepts.map(c=>({...c,page:p.page,section:pageLink(p.page),status:p.page===24&&c.text.includes('Figure 7')?'partly explained; fine diagram transcription deferred':'explained'})));
if(inventory.length!==135||notes.size!==135||sessions.length!==36)throw Error('Incomplete source or course');
for(let p=1;p<=135;p++)if(!notes.has(p)||!lessonFor(p)||inventory[p-1].page!==p)throw Error(`Missing page ${p}`);
fs.mkdirSync(out,{recursive:true});
const nav=`<nav aria-label="Study companion"><a href="index.html">Course</a><a href="source-map.html">Source map</a><a href="coverage.html">Coverage audit</a><a href="glossary.html">Glossary</a><a href="evidence.html">Science comparisons</a></nav>`;
function write(file,title,body){fs.writeFileSync(`${out}/${file}`,`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} · Biodynamics study companion</title><meta name="description" content="A source-mapped study of What Is Biodynamics?, with page-by-page explanations, highlighted ideas and understanding questions."><link rel="stylesheet" href="../../site-watercolour.css"><link rel="stylesheet" href="companion.css"></head><body><a class="skip" href="#main">Skip to content</a><header><a class="brand" href="../../index.html">Anthroposophy</a><a href="../index.html">Biodynamics overview</a></header>${nav}<main id="main"><h1>${esc(title)}</h1>${body}</main><footer><a href="source-map.html">Return to the source map</a><a href="../../pt/biodynamics/index.html" lang="pt-BR">Curso breve em português</a></footer></body></html>`);}
const readingMethod=`<section class="panel"><h2>A method for close reading</h2><ol><li>Read the assigned captures in your copy, including the chapter introduction and notes.</li><li>Identify who is speaking and reconstruct the claim, terms, reasons and qualifications.</li><li>Read the page commentary. Distinguish an observation, an analogy, an asserted premise and a demonstrated result.</li><li>Answer the lesson question before opening the explanation. Revise your answer with page references.</li></ol><p>All examples labeled “Teaching example” are original instructor examples. They illustrate reasoning; they are not reported events from the book. Use your own notebook. These new companion pages do not save responses.</p></section>`;
const courseMap=chapters.map(c=>`<section id="chapter-${c.id}"><h2>${esc(c.title)}</h2><p class="meta">PDF captures ${c.start}–${c.end}</p><ol class="sessions">${sessions.filter(s=>s.chapter===c.id).map(s=>`<li><a href="session-${s.id}.html"><span class="meta">Lesson ${s.id} · PDF ${s.start}–${s.end}</span><strong>${esc(s.title)}</strong><span>${esc(s.goal)}</span></a></li>`).join('')}</ol></section>`).join('');
write('index.html','What Is Biodynamics? — detailed study companion',`<p class="lead">36 lessons • 135 page commentaries • ${concepts.length} inventory items • 11 highlighted ideas</p><p>Study Courtney’s introduction and all seven chapters in the book’s order. The source map comes first; the lessons then explain the reasoning page by page, including unhighlighted context, discussions and editorial qualifications.</p><div class="actions"><a class="button" href="source-map.html">Start with the source map</a><a href="session-00.html">Begin lesson 00 →</a><a href="study-companion.md" download>Download the complete companion (.md)</a></div><section class="panel"><h2>What “biodynamics” means in this book</h2><p>Courtney presents a spiritual approach to farming organized around the farm as a living individuality, relationships with the wider cosmos, and specially made preparations. The selected Steiner lectures supply a spiritual account of perception and nature, then agricultural proposals. Understanding the book means explaining those connections while distinguishing its assertions from independently established findings.</p><p>The course assesses understanding, attribution and reasoned judgment. Agreement with its spiritual claims is not required. It is a reading course, with observation and paper-based exercises, rather than instructions to carry out the historical treatments.</p></section>${readingMethod}<section><h2>Plan your study</h2><p>Allow roughly 45–75 minutes per lesson, including reading and written response; dense sections may take longer. One or two lessons per meeting gives an 18–36 meeting route. Lessons 00 and 35 provide orientation and final assessment. The introduction receives seven lessons because its arguments cannot be reduced to a preface summary.</p><p>This expansion is in English. The existing shorter course is available in both languages. <a href="coverage.html#limits">Read the explicit coverage limits</a> before using the companion as a source-completeness claim.</p></section>${courseMap}<section id="assessment"><h2>Final portfolio</h2><ol><li>Write a 500–800 word definition of biodynamics as presented in this book, distinguishing Courtney, Means and Steiner.</li><li>Explain each of the eleven highlighted ideas, including its surrounding argument and page reference.</li><li>Choose one argument from every numbered chapter. State its premises, example or reason, conclusion and any unresolved step.</li><li>Compare two source claims with the linked scientific explanations. Explain the actual disagreement.</li><li>Create a garden or farm portrait separating your observations from the book’s interpretations. Design a hypothetical comparison with specified outcomes and alternative explanations.</li><li>List remaining uncertainties and identify the additional sources needed to investigate them.</li></ol><p><strong>Assessment:</strong> source accuracy 30%; reasoning and terms 30%; qualifications and evidence distinctions 25%; clear teaching examples and questions 15%. Full credit is available without accepting a spiritual claim. A name or passing mention does not count as an explanation.</p></section>`);

const search=`<label for="source-search">Find a concept, page or inventory ID</label><input id="source-search" type="search" placeholder="Try yarrow, conscience, 101 or C024"><p id="search-count" role="status" aria-live="polite"></p><script defer src="source-search.js"></script>`;
write('source-map.html','Source map — before the lessons',`<p class="lead">Every capture page, in the book’s structure, with its substantive ideas and assigned lesson.</p><section class="panel"><h2>Edition and reference system</h2><p>Rudolf Steiner, <cite>What Is Biodynamics? A Way to Heal and Revitalize the Earth. Seven Lectures</cite>, SteinerBooks. Introduction © 2005 Hugh J. Courtney. Edited by Marcia Merryman Means. The supplied PDF contains 135 capture pages; these are not printed page numbers.</p><p>Source cycles: GA 136 lectures 1–2 (April 3–4, 1912); GA 230 lectures 7–8 (November 2–3, 1923); GA 327 lectures 4–6 (June 12–14, 1924). Page 3 supplies translator and editor credits. This is a selection, not the complete Agriculture Course.</p><p>The inventory was drafted before the page lessons. The text extraction was read throughout; all page images were screened for highlights and layout, with highlighted passages inspected at full size. The private book and its page images are not reproduced here.</p></section>${search}${chapters.map(c=>`<section class="map-chapter" id="chapter-${c.id}"><h2>${esc(c.title)}</h2>${inventory.filter(p=>p.page>=c.start&&p.page<=c.end).map(p=>`<article class="inventory-page" id="inventory-${p.page}"><h3>PDF ${p.page} · <a href="${pageLink(p.page)}">Lesson ${lessonFor(p.page).id}: explanation</a></h3><ul>${p.concepts.map(v=>`<li><span class="concept-id">${v.id}</span> ${esc(v.text)}</li>`).join('')}</ul>${highlights.some(h=>h.page===p.page)?'<p class="highlight">Contains a required highlighted passage. See the highlight audit.</p>':''}</article>`).join('')}</section>`).join('')}`);

for(const [i,s] of sessions.entries()){
 const pages=inventory.filter(p=>p.page>=s.start&&p.page<=s.end);
 const localHighlights=highlights.filter(h=>h.page>=s.start&&h.page<=s.end);
 const notesHtml=pages.map(p=>`<section class="page-commentary" id="page-${p.page}"><p class="eyebrow">Source commentary · PDF capture ${p.page}</p><h2>${esc(p.concepts[0].text)}</h2><p class="meta"><a href="source-map.html#inventory-${p.page}">Inspect this page’s inventory</a></p>${para(notes.get(p.page))}${highlights.filter(h=>h.page===p.page).map(h=>`<aside class="highlight" id="${h.id}"><strong>Required highlight ${h.id} · ${esc(h.voice)}</strong><p>${esc(h.idea)}</p><p>${esc(h.teaching)}</p><a href="coverage.html#${h.id}">Coverage record</a></aside>`).join('')}${evidence.filter(e=>e.pages.includes(p.page)).map(e=>`<aside class="science"><strong>Scientific comparison · ${esc(e.title)}</strong><p>${esc(e.text)}</p><p><a href="${e.url}">${esc(e.label)}</a>${e.extra?` · <a href="${e.extra[1]}">${esc(e.extra[0])}</a>`:''} · <a href="evidence.html#${e.id}">Comparison ${e.id}</a></p></aside>`).join('')}</section>`).join('');
 write(`session-${s.id}.html`,s.title,`<p class="eyebrow">Lesson ${s.id} · ${esc(chapters.find(c=>c.id===s.chapter).title)}</p><p class="lead">${esc(s.goal)}</p><p class="meta">Read PDF captures ${s.start}–${s.end} in your copy. Suggested study time: 45–75 minutes plus any additional reflection.</p><nav class="page-links" aria-label="Pages in this lesson">${pages.map(p=>`<a href="#page-${p.page}">PDF ${p.page}</a>`).join('')}</nav>${localHighlights.length?`<p class="highlight">This lesson explains ${localHighlights.length} required highlighted ideas.</p>`:''}<section class="panel"><h2>Before reading</h2><p>In your notebook, identify the speaker and write what you think this section is trying to explain. As you read, mark each reason as an observation, analogy, reported testimony or asserted spiritual premise. Return to <a href="glossary.html">the glossary</a> when a technical term changes your understanding.</p></section>${notesHtml}<section class="panel" id="teaching-example"><h2>Teaching example — introduced by the instructor</h2><p>${esc(s.example)}</p></section><section id="understanding"><h2>Check your understanding</h2><p><strong>${esc(s.question)}</strong></p><p>Write your answer before opening the explanation. Cite a page and distinguish the source’s claim from your judgment.</p><details><summary>Read the explained answer</summary><p>${esc(s.answer)}</p></details><h3>Explain one more passage</h3><p>Select a different inventory item from this lesson. Define its key term, reconstruct the reasoning, identify the example or support supplied, and retain one qualification. If support is not supplied, say so. Then explain how the item connects to the preceding lesson or page.</p><details><summary>What a complete answer includes</summary><p>A complete answer names the voice and capture page, explains the concept in its local context, states the connection between premise and conclusion, and distinguishes supplied support from your interpretation. Merely listing a preparation, elemental or planet is insufficient.</p></details></section><nav class="lesson-navigation" aria-label="Lesson sequence">${i?`<a href="session-${sessions[i-1].id}.html">← Previous lesson</a>`:'<a href="source-map.html">← Source map</a>'}<a href="index.html#chapter-${s.chapter}">Chapter path</a>${i<sessions.length-1?`<a href="session-${sessions[i+1].id}.html">Next lesson →</a>`:'<a href="index.html#assessment">Final portfolio →</a>'}</nav>`);
}

write('glossary.html','Terms in context',`<p>Definitions follow this book’s use. They explain a framework without assuming that its spiritual entities are scientifically established.</p><dl>${glossary.map(([term,definition,pages])=>`<dt>${esc(term)}</dt><dd><p>${esc(definition)}</p><p>${pages.map(p=>`<a href="${pageLink(p)}">PDF ${p}</a>`).join(' · ')}</p></dd>`).join('')}</dl>`);
write('evidence.html','Spiritual claims and scientific comparisons',`<p class="lead">Keep the explanation of a claim separate from the evidence for accepting it.</p><p>The commentary reconstructs the book’s spiritual and philosophical claims. Instructor examples explain reasoning. The comparisons below identify specific established findings or historical corrections with external sources checked September 17, 2026. They do not constitute a systematic review of biodynamic agriculture.</p>${evidence.map(e=>`<section class="science" id="${e.id}"><h2>${e.id} · ${esc(e.title)}</h2><p>${esc(e.text)}</p><p><a href="${e.url}">${esc(e.label)}</a>${e.extra?` · <a href="${e.extra[1]}">${esc(e.extra[0])}</a>`:''}</p><p>Relevant commentary: ${e.pages.map(p=>`<a href="${pageLink(p)}">PDF ${p}</a>`).join(' · ')}</p></section>`).join('')}`);

const highlightTable=highlights.map(h=>`<tr id="${h.id}"><th scope="row">${h.id}</th><td>PDF ${h.page}<br>Capture starts at Kindle location ${h.location}<br>${esc(h.voice)}</td><td>${esc(h.idea)}<p>${esc(h.teaching)}</p></td><td><a href="${pageLink(h.page)}">Lesson ${lessonFor(h.page).id}, PDF ${h.page}</a><br>Explained with context</td></tr>`).join('');
write('coverage.html','Coverage audit and remaining limits',`<p class="lead">135/135 pages mapped · 11/11 highlighted ideas explained · ${concepts.length} inventory items assigned</p><p>This audit links to explanatory paragraphs, not just topic names. Most inventory items are explained in their page commentary; the fine transcription of Figure 7 remains explicitly partial. Automated checks verify completeness of references and structure, while interpretation and explanatory adequacy require editorial judgment.</p><section id="highlights"><h2>Every highlighted idea</h2><p>Four visible yellow passages were found in the supplied PDF: pages 76, 101, 102 and 114. They contain eleven separately mapped ideas. The last passage is by Means, not Steiner. The page commentary restores unhighlighted context and does not rely on the damaged OCR wording.</p><div class="table-scroll"><table><thead><tr><th>ID</th><th>Source</th><th>Distinct idea and teaching treatment</th><th>Explanation</th></tr></thead><tbody>${highlightTable}</tbody></table></div></section><section id="limits"><h2>Omissions, qualifications and deferred material</h2><ul>${limits.map(l=>`<li>${esc(l)}</li>`).join('')}</ul></section><section><h2>Concept-by-concept crosswalk</h2><p>Each inventory identifier refers to the map drafted before lessons. Items sharing a page point to the same close-reading section, where their relationships are explained together.</p><div class="table-scroll"><table><thead><tr><th>ID / page</th><th>Inventory item</th><th>Teaching section</th><th>Status</th></tr></thead><tbody>${concepts.map(c=>`<tr><th scope="row">${c.id}<br>PDF ${c.page}</th><td>${esc(c.text)}</td><td><a href="${c.section}">Lesson ${lessonFor(c.page).id}, PDF ${c.page}</a></td><td>${esc(c.status)}</td></tr>`).join('')}</tbody></table></div></section>`);

const md=['# What Is Biodynamics? — complete study companion','English expansion · 17 September 2026','## Source and method','Rudolf Steiner, What Is Biodynamics? A Way to Heal and Revitalize the Earth. Seven Lectures. SteinerBooks. Introduction © 2005 Hugh J. Courtney; edited by Marcia Merryman Means. References count the 135 PDF captures, not printed pages. Original teaching commentary follows the supplied source.','## Source map (prepared before lessons)',...chapters.flatMap(c=>[`### ${c.title}`, ...inventory.filter(p=>p.page>=c.start&&p.page<=c.end).map(p=>`- PDF ${p.page} → Lesson ${lessonFor(p.page).id}: ${p.concepts.map(v=>v.id+' '+v.text).join('; ')}`)]),'## Highlight audit',...highlights.map(h=>`- ${h.id}: PDF ${h.page}, ${h.voice}. ${h.idea} Taught in Lesson ${lessonFor(h.page).id}, PDF ${h.page}. ${h.teaching}`),'## Lessons',...sessions.flatMap(s=>[`### Lesson ${s.id}: ${s.title}`,`Read PDF ${s.start}–${s.end}. ${s.goal}`,...inventory.filter(p=>p.page>=s.start&&p.page<=s.end).flatMap(p=>[`#### PDF ${p.page}`,notes.get(p.page)]),'#### Teaching example — introduced by the instructor',s.example,'#### Understanding question',s.question,'#### Explained answer',s.answer]),'## Glossary',...glossary.map(([t,d,p])=>`- **${t}:** ${d} (PDF ${p.join(', ')})`),'## Scientific comparisons',...evidence.map(e=>`### ${e.id}: ${e.title}\n\n${e.text}\n\n[${e.label}](${e.url})${e.extra?`; [${e.extra[0]}](${e.extra[1]})`:''}`),'## Coverage crosswalk',...concepts.map(c=>`- ${c.id} (PDF ${c.page}): ${c.text} → Lesson ${lessonFor(c.page).id}, PDF ${c.page}. ${c.status}.`),'## Limits and deferred work',...limits.map(l=>'- '+l),'## Final assessment','Explain the book’s definition, all eleven highlighted ideas, and one argument from every chapter. Compare two claims with scientific sources. Create a garden portrait separating observation from interpretation and design a hypothetical test. Assess source accuracy (30%), reasoning (30%), qualifications (25%) and clear teaching (15%). Agreement with spiritual claims is not required.'].join('\n\n');
fs.writeFileSync(`${out}/study-companion.md`,md);
fs.writeFileSync('content/biodynamics-coverage.json',JSON.stringify({reviewed:'2026-09-17',pdfPages:135,sessionCount:sessions.length,conceptCount:concepts.length,highlights:highlights.map(h=>({...h,section:pageLink(h.page),status:'explained'})),concepts,limits},null,2)+'\n');
for(const lang of ['en','pt']){
 const root=lang==='en'?'docs':'docs/pt';
 for(const filename of ['index.html',...Array.from({length:9},(_,i)=>`lessons/${String(i).padStart(2,'0')}.html`)]){
  const file=`${root}/biodynamics/${filename}`;
  if(!fs.existsSync(file))continue;
  let html=fs.readFileSync(file,'utf8').replace(/<!-- biodynamics-companion:start -->[\s\S]*?<!-- biodynamics-companion:end -->/g,'');
  const href=lang==='en'?(filename==='index.html'?'companion/index.html':'../companion/index.html'):(filename==='index.html'?'../../biodynamics/companion/index.html':'../../../biodynamics/companion/index.html');
  const block=`<!-- biodynamics-companion:start --><section class="source-note"><h2>${lang==='en'?'Detailed page-by-page companion':'Estudo detalhado página a página (em inglês)'}</h2><p>${lang==='en'?'36 lessons explain all 135 source pages, with a source inventory, eleven highlighted ideas, glossary and coverage audit.':'36 lições em inglês explicam as 135 páginas da fonte, com inventário, onze ideias destacadas, glossário e auditoria de cobertura.'}</p><p><a href="${href}">${lang==='en'?'Open the detailed study companion':'Abrir o estudo detalhado em inglês'} →</a></p></section><!-- biodynamics-companion:end -->`;
  html=html.replace('</main>',block+'</main>');fs.writeFileSync(file,html);
 }
}
console.log(`Built biodynamics companion: ${sessions.length} lessons, ${notes.size} page explanations, ${concepts.length} inventory items, ${highlights.length} highlighted ideas.`);
